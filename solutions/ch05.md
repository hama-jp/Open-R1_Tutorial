# 第5章 解答

## 演習 1: 10 件の自作データで SFT

### 完全解
[`examples/ch05/minisft.py`](../examples/ch05/minisft.py) を参照。
`SFTTrainer` は `messages` 列を自動で chat template に流してくれる。

学習後に推論してみると、未学習時には長々と雑談を生成するモデルが、
**短く指示に沿った回答** を返すようになっているはず。

## 演習 2: `<think>` タグで思考過程を誘導

サンプルの `assistant` 文を次のように書き換えるだけ。

```python
{"role": "assistant",
 "content": "<think>2と7を掛けると14。</think>答えは14です。"}
```

`packing=True, max_seq_length=1024` にしてから 3 epoch 学習させると、
推論時に `<think>…</think>…` 形式で生成するようになる。

## 演習 3: Open-R1 スクリプトとの対応

`open-r1/src/open_r1/sft.py` 冒頭:

```python
config = yaml.safe_load(open(args.config))
trainer = SFTTrainer(
    model=config["model_name_or_path"],
    args=SFTConfig(**config),
    train_dataset=load_dataset(config["dataset_name"], split="train"),
    tokenizer=AutoTokenizer.from_pretrained(config["model_name_or_path"]),
)
trainer.train()
```

- `recipes/OpenR1-Distill-7B/sft/config.yaml` の `max_seq_length: 32768` → 本章 5.6 の長文対応
- `gradient_checkpointing: true` + `deepspeed: configs/zero3.json` → 11章で扱う ZeRO-3
- `packing: true` → サンプル詰め込み、9章で扱う
