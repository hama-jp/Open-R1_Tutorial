# 第11章 環境構築とミニチュア実験

ここからは **手を動かす** 章です。
まずは Open-R1 を動かすための環境を整え、
GPU に優しい小型モデルで **SFT のミニ実験** までを通しで体験します。

## 11.1 必要な道具一式

| カテゴリ | 推奨 |
|---|---|
| OS | Ubuntu 22.04 / 24.04 |
| Python | 3.11 系 |
| CUDA | 12.1 以上 |
| GPU | 24GB 以上（RTX 3090 / 4090 / A100 / H100 等） |
| ストレージ | 100GB 以上（チェックポイント保存のため） |
| 周辺 | `git`, `uv`, `build-essential` |

GPU がない場合: 12.x 章は読むだけで構いません。
11.x 章の **SFT ミニ実験** は Google Colab T4（15GB）で動きます。

## 11.2 `uv` のインストール

HuggingFace チームは依存管理に **`uv`** を使っています。

```bash
$ curl -LsSf https://astral.sh/uv/install.sh | sh
$ uv --version
```

`pip` でも動きますが、本書は `uv` を前提に進めます。

## 11.3 open-r1 を clone して依存解決

```bash
$ git clone https://github.com/huggingface/open-r1.git
$ cd open-r1
$ uv venv .venv --python 3.11
$ source .venv/bin/activate
$ uv pip install -e ".[dev,eval]"
```

これで **`trl`, `transformers`, `deepspeed`, `vllm`, `lighteval`** など
一式がまとめて入ります。

```bash
$ python -c "import trl, transformers; print(trl.__version__, transformers.__version__)"
0.14.0 4.48.0
```

## 11.4 Hugging Face 認証

モデル・データセットのダウンロードに使います。

```bash
$ huggingface-cli login
# トークンを貼り付け。Write 権限付きが便利
```

## 11.5 動作確認: まずは推論

DeepSeek-R1-Distill-Qwen-1.5B は 3GB 程度で、多くの環境で動きます。

```python
# examples/ch11/quick_infer.py
from transformers import AutoTokenizer, AutoModelForCausalLM
import torch

mid = "deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B"
tok = AutoTokenizer.from_pretrained(mid)
model = AutoModelForCausalLM.from_pretrained(
    mid, torch_dtype=torch.bfloat16, device_map="auto"
)

prompt = tok.apply_chat_template(
    [{"role": "user", "content": "1/3 + 1/6 を計算せよ。"}],
    tokenize=False, add_generation_prompt=True,
)
inputs = tok(prompt, return_tensors="pt").to(model.device)
out = model.generate(**inputs, max_new_tokens=1024, temperature=0.6, top_p=0.95)
print(tok.decode(out[0], skip_special_tokens=True))
```

`<think>` で始まる思考過程と、その後の最終回答 `1/2` が得られれば成功です。

## 11.6 ミニ SFT 実験：小さな算数データで試す

### 11.6.1 データセット作成

まず小さな算数データを自作します。

```python
# examples/ch11/make_tiny_ds.py
import json, random, pathlib
random.seed(0)

rows = []
for _ in range(200):
    a, b = random.randint(1, 50), random.randint(1, 50)
    rows.append({
        "messages": [
            {"role": "user", "content": f"{a} + {b} はいくつですか？"},
            {"role": "assistant",
             "content": f"<think>{a}と{b}を足すと{a+b}。</think>答えは{a+b}です。"},
        ]
    })

pathlib.Path("tiny_addition.jsonl").write_text(
    "\n".join(json.dumps(r, ensure_ascii=False) for r in rows)
)
```

### 11.6.2 SFT スクリプト

```python
# examples/ch11/tiny_sft.py
from trl import SFTTrainer, SFTConfig
from transformers import AutoTokenizer, AutoModelForCausalLM
from datasets import load_dataset
import torch

model_id = "Qwen/Qwen2.5-0.5B-Instruct"
tok = AutoTokenizer.from_pretrained(model_id)
model = AutoModelForCausalLM.from_pretrained(model_id, torch_dtype=torch.bfloat16)

ds = load_dataset("json", data_files="tiny_addition.jsonl", split="train")

config = SFTConfig(
    output_dir="out-sft",
    num_train_epochs=1,
    learning_rate=2e-5,
    per_device_train_batch_size=2,
    gradient_accumulation_steps=4,
    bf16=True,
    logging_steps=10,
    max_seq_length=512,
    packing=False,
)

trainer = SFTTrainer(
    model=model,
    args=config,
    tokenizer=tok,
    train_dataset=ds,
)
trainer.train()
trainer.save_model("out-sft/final")
```

T4 GPU（15GB）でも 5〜10 分で終わります。

### 11.6.3 学習前後の比較

```python
# examples/ch11/compare.py
from transformers import AutoTokenizer, AutoModelForCausalLM
import torch

def ask(path, q):
    tok = AutoTokenizer.from_pretrained(path)
    m = AutoModelForCausalLM.from_pretrained(path, torch_dtype=torch.bfloat16).to("cuda")
    prompt = tok.apply_chat_template(
        [{"role": "user", "content": q}], tokenize=False, add_generation_prompt=True
    )
    ids = tok(prompt, return_tensors="pt").to("cuda")
    out = m.generate(**ids, max_new_tokens=200, temperature=0.0)
    return tok.decode(out[0], skip_special_tokens=True)

q = "17 + 28 はいくつ？"
print("BEFORE:", ask("Qwen/Qwen2.5-0.5B-Instruct", q))
print("AFTER :", ask("out-sft/final",            q))
```

SFT 後は `<think>` タグを使って丁寧に足し算を解くようになるはずです。

## 11.7 よくあるエラーと対処

| エラー | 原因 | 対処 |
|---|---|---|
| `torch.cuda.OutOfMemoryError` | バッチ・系列長が大きすぎ | `per_device_train_batch_size=1`, `gradient_accumulation_steps`↑ |
| `bf16 not supported` | Ampere 未満（T4 等） | `fp16=True` に変更 |
| `No module named 'flash_attn'` | FlashAttention 未導入 | `attn_implementation="sdpa"` に変更 |
| `Transformers version mismatch` | trl / transformers 不整合 | `uv pip sync` で再インストール |
| `HFValidationError: ... gated` | アクセス許可が必要なモデル | HF 上で同意してから再試行 |

## 11.8 DeepSpeed ZeRO-3 を使う

本番の 7B 学習には **DeepSpeed ZeRO-3** が必須です。Open-R1 は設定ファイルを同梱しています。

```bash
$ accelerate launch \
    --config_file configs/zero3.yaml \
    src/open_r1/sft.py --config recipes/OpenR1-Distill-7B/sft/config.yaml
```

ZeRO-3 は **オプティマイザ状態・勾配・モデルパラメータ** を全 GPU に分散して持たせる方式で、
8× H100 80GB で 7B モデルを学習できるのはこの仕組みのおかげです。

## 11.9 まとめ

- `uv` + `open-r1` の clone が最短ルート
- まずは 1.5B Distill モデルで推論確認
- Qwen2.5-0.5B を使った **ミニ SFT** は Colab T4 で動かせる
- OOM 対処・DeepSpeed 設定のコツを把握すれば、本番サイズにも拡張できる

次章では、いよいよ **GRPO を自分で回して "aha moment" を観察** します。

## 🧪 手を動かしてみよう

1. 本章の `quick_infer.py` を走らせ、**`<think>` 内の思考** と **最終回答** を
   正規表現で分離してコンソールに色分け表示するスクリプトを作ってみてください。

2. Qwen2.5-0.5B で引き算・掛け算のミニ SFT データを足し、
   4 種混合（add/sub/mul/div）で 1 epoch 学習。
   学習前後で各演算の正答率がどう変化するか計測してください。

3. DeepSpeed ZeRO-0/1/2/3 の違いを、`configs/zeroN.json` を読み比べてメモしてください。
   （パラメータ/勾配/オプティマイザのどこが分散されるか）

---

[← 第10章 データセットと評価](ch10.md) ｜ [→ 第12章 実践GRPO](ch12.md)
